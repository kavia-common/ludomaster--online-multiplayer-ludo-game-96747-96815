# REST API base URL (e.g., http://localhost:4000/api)
REACT_APP_API_BASE=/api

# WebSocket base URL (e.g., ws://localhost:4000/ws). If not set, derived from API base.
# REACT_APP_WS_BASE=ws://localhost:4000/ws

# Public site URL for email redirect (if backend uses email verification flows)
REACT_APP_SITE_URL=http://localhost:3000
