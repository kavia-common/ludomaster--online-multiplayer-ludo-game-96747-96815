# PUBLIC_INTERFACE
# Required environment variables for LudoMaster Frontend
# Copy to .env and fill as needed.

# Base URL for REST API (e.g., https://api.example.com or /api when proxied)
REACT_APP_API_BASE=/api

# Base URL for WebSocket (e.g., wss://api.example.com/ws). If omitted, derived from API base.
REACT_APP_WS_BASE=

# Public site URL for auth email redirects (if backend uses email link flows)
REACT_APP_SITE_URL=http://localhost:3000
